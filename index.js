/**
 * EasyEcom API Workflow
 *
 * Commands:
 *   npm run fetch-all-returns  — Fetch getAllReturns + getPendingReturns,
 *                                enrich with Shopify customer names,
 *                                save to all_returns_enriched.json
 *
 *   npm run shopify-auth       — Regenerate Shopify access token
 */
main();
